# git-push 闸门白名单（入库共享）
# 语法：path:<glob> 放行路径 / fingerprint:<sha1> 精确放行单条命中 / regex:<re> / extra:<glob> 追加敏感模式
# 详见 git-push skill 的 references/privacy-checklist.md
#
# memory/README.md 只是记忆目录的说明文件，无隐私数据，刻意随仓库分发（.gitignore 里 !/memory/README.md）
path:memory/README.md

# skills/zhihu/references/http-api.md 里有一个 11 位的知乎 ContentID（文章 ID，同值也出现在
# zhuanlan.zhihu.com/p/<ContentID> 链接中）被规则误判为手机号；该目录第三方托管、升级会覆盖，故不改文件
# （此处刻意不回显该数字，否则本文件自己会被 pii-phone-cn 命中）
fingerprint:061bec1bf2acb9fca7b95824546f8835e89e3ada
